#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @number: the int to check
 * Return: the absolute value of int
 */
int _abs(int number)
{
	if (number >= 0)
	{
		return (number);
	}
	return (-number);
}
